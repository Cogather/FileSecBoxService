spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Server port
server.port=8003

# Logging
logging.level.com.example.filesecbox=INFO

# Sandbox paths
app.product.root=D:/webIde/product
app.skill.root=${app.product.root}/skill
app.sandbox.root=${app.product.root}/sandbox

